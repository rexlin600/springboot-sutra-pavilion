spring:
  application:
    name: reddison
  redis:
    database: 0
    host: 127.0.0.1
    port: 6379
    lettuce:
      pool:
        min-idle: 5
        max-active: 8
        max-idle: 8
        max-wait: 10000
#  profiles:
#    active: redisson  # redisson外部配置文件
server:
  port: 10026