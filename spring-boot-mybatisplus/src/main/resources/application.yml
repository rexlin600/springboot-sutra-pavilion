spring:
  application:
    name: mybatisplus
  datasource:
    driver-class-name: org.h2.Driver
    schema: classpath:db/schema-h2.sql
    data: classpath:db/data-h2.sql
    url: jdbc:h2:mem:test
    username: root
    password: test
mybatis-plus:
  type-aliases-package: xyz.rexlin600
  mapper-locations: classpath*:/mapper/**/*.xml
server:
  port: 10008